#!/bin/bash
# loom session-start hook — thin wrapper
# Core logic in handlers/session-start.js, executed via run-hook.js

HOOK_DIR="$(cd "$(dirname "$0")" && pwd)"
exec node "$HOOK_DIR/run-hook.js" session-start
