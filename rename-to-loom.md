请对当前项目执行一次完整的重命名改造，将产品名从 rss 统一迁移为 Loom。
改造目标
对外品牌统一为 Loom
CLI 主命令统一为 loom
所有使用 .rss/ 的地方改为 .loom/
所有文档、模板、测试、输出文案同步更新
执行要求
先全局扫描并列出所有以下残留引用：
RSS
rss
RSD
Requirement Spec Driven
.rss
按类别输出改造清单：
用户可见文案
CLI 命令
文件路径
内部变量/函数/类型
模板
测试
然后按以下顺序实施：
品牌文案替换
CLI 重命名
默认目录迁移到 .loom/

内部命名重构
更新测试
所有改动尽量保持最小侵入
最终输出：
改动文件列表
关键设计说明
兼容策略说明
验收结果
关键兼容策略

额外要求
不要只做简单字符串替换
